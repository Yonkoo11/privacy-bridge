node_modules/
.env
build/
out/
.next/
*.ptau
*.zkey
*.wasm
*.r1cs
*.sym

# Allow circuit artifacts needed by the web app (trusted setup outputs)
!app/public/bridge.wasm
!app/public/bridge_final.zkey
target/
.snfoundry_cache/

# Secret note files (contain deposit secrets — never commit)
note-*.json
proof-*.json
receipt-*.json

# Deployment files (contain local addresses, regen with deploy scripts)
deploy.json
deploy-flow.json
